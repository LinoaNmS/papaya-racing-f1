enum TeamTheme {
  mclaren,
  redBull,
  ferrari,
  racingBulls,
  mercedes,
  williams,
  haas,
  alpine,
  strake,
  astonMartin,
}

enum PRButtonVariant { filled, outlined, text }

enum PRButtonSize { small, medium, large }

// shared
enum PRColor { primary, secondary, success, danger, warning, info }
